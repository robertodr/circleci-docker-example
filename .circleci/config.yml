version: 2
jobs:
  build:
    docker:
      - image: robertodr/fuffa
        #entrypoint:
        #  - "echo Hellllllllloooooo"
        #command: ["/bin/bash"]
    steps:
      - checkout
      - run: echo "A first hello from $PATH"
      - run: python3 --version
